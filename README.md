# CommentLab

CommentLab 是一个面向普通个人和内容创作者的发布前沟通风险模拟系统。它通过多种受众 Agent 模拟帖子发布后的评论、回复和热度变化，识别误解、负面情绪、阵营争吵和话题跑偏风险，在保持发布者人设的前提下生成改写版本，并使用同一批受众进行反事实重模拟。

> 本系统用于发布前沟通风险压力测试，模拟结果不代表真实舆情预测。

## 第一版边界

- 输入为 500 字以内中文短文本和简版发布者画像。
- 面向个人创作者，不面向政府、学校行政或大型企业危机公关。
- 不判断观点对错，不做事实核查。
- 不读取链接、图片、视频、账号历史或真实用户画像。
- 平台无关，不模拟某个具体平台的语言风格。
- 不使用外部评论语料。数据集、平台风格和语料检索留作后续阶段。

## 系统流程

```text
内容分析 Agent
→ 受众规划 Agent
→ 用户确认受众比例
→ 评论生成 Agent（三轮）
→ Python 热度模拟工具
→ 风险诊断 Agent
→ 人设保持改写 Agent
→ 相同受众反事实重模拟
→ 对比评估 Agent
```

评论生成每轮批量处理多个 Persona。约 50 个潜水用户完全由 Python 规则模拟点赞，避免额外模型调用。正常完整流程约 11 次模型调用；相同请求写入 SQLite 缓存。

## Agent 与工具

- **内容分析 Agent**：提取核心信息、模糊表达、缺失信息、潜在误解和人设冲突。
- **受众规划 Agent**：从 12 种预设 Persona 中规划受众构成。
- **评论生成 Agent**：按不同阅读深度、信任和情绪倾向批量生成评论、回复或沉默行为。
- **热度模拟工具**：计算潜水点赞、回复数、热度和可见评论，全程不调用模型。
- **风险诊断 Agent**：识别误解、负面情绪、对立和跑题，定位原文片段与误解链。
- **人设保持改写 Agent**：不改变观点、不添加事实，保留个人表达风格。
- **对比评估 Agent**：比较同一受众和随机配置下改写前后的模拟结果。

## 安装

服务器需要 Python 3.10 或更高版本。

服务器缺少系统 `python3-venv` 时，使用项目独立 Conda 环境：

```bash
cd ~/Big_task
~/miniconda3/bin/conda create -y -n commentlab python=3.12 pip
~/miniconda3/envs/commentlab/bin/python -m pip install \
  -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
```

如果系统已提供 `python3-venv`，也可以在项目中创建 `.venv`。

## 配置

复制环境变量模板并填写 OpenAI-compatible 服务：

```bash
cp .env.example .env
```

```env
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=deepseek-v4-flash
LLM_TEMPERATURE=0.4
LLM_TIMEOUT=90
DEMO_MODE=false
DATABASE_PATH=commentlab.db
WEB_SEARCH_ENABLED=true
WEB_SEARCH_MODEL=qwen3.6-flash
```

任何一项必要模型配置为空时，系统都会进入 `DEMO_MODE`，不会因为缺少密钥而崩溃。真实密钥不得提交到 Git 或出现在截图中。

## DEMO_MODE

`DEMO_MODE=true` 时，Agent 使用符合相同 Pydantic 契约的确定性启发式实现。它不是静态页面：三轮状态推进、Persona 激活、潜水点赞、热度、风险权重、反事实一致性和 SQLite 持久化仍会真实执行。

内置三个课堂案例：

1. `最近会适当减少更新，大家不用多想。`
2. `有些人真的应该学会尊重别人，不要什么事情都来指手画脚。`
3. `我承认这件事处理得不够好，但当时的情况真的很复杂，希望大家不要只看结果。`

## 运行

```bash
~/miniconda3/envs/commentlab/bin/streamlit run app.py \
  --server.address 0.0.0.0 --server.port 8502
```

当前课程服务器上的 `8501` 已被其他进程占用，因此使用 `8502`。在 VS Code Remote SSH 的“端口”面板转发 `8502`，然后打开：

```text
http://127.0.0.1:8502
```

如果端口被其他进程占用，使用 `8502`，不要终止其他用户的服务。

## 测试

```bash
cd ~/Big_task
~/miniconda3/envs/commentlab/bin/python -m pytest -q
```

测试覆盖数据契约、500 字限制、比例归一化、热度、潜水点赞、随机种子、三轮模拟、无效回复、25/75 风险权重、风险阈值、SQLite、缓存、结构化 JSON 修复、DEMO_MODE、历史加载、三个案例和反事实受众一致性。

## SQLite

数据库包含：

- `projects`：项目输入、改写与完整结果快照；
- `personas`：实际模拟使用的受众配置；
- `comments`：改写前后全部评论和内部分析字段；
- `reports`：前后风险报告和对比报告；
- `llm_cache`：相同模型请求的结构化输出缓存。

历史结果直接从 `projects.result_json` 恢复，不再次调用模型。

## 课堂演示建议

1. 提前启动 Streamlit，并确认侧栏显示 `DEMO_MODE` 或可用模型名。
2. 选择“容易被视为辩解的回应”案例。
3. 展示内容初步分析和受众比例。
4. 运行三轮模拟，查看高热评论、风险句和误解链。
5. 打开改写对比，说明前后使用相同 Persona、seed、轮数和热度规则。
6. 从历史结果重新打开刚才的项目，展示无需再次调用模型。

## 降级与失败处理

- 模型调用超时为 30 秒，失败后重试一次。
- 结构化输出由 Pydantic 校验；非法 JSON 可修复尾随逗号和代码围栏。
- 单个 Agent 阶段连续失败后使用本地降级结果，已有流程状态不会丢失。
- 缺少模型配置时自动进入 DEMO_MODE。
- 相同输入和模型配置优先读取 SQLite 缓存。

## 评测建议

- **功能完整性**：确认输入到反事实对比的完整闭环。
- **结构稳定性**：统计字段缺失、非法 JSON、重复评论和无效回复。
- **Persona 区分度**：人工判断各 Persona 的阅读方式和表达是否不同。
- **风险点覆盖率**：系统识别的主要风险点 / 人工标注主要风险点。
- **改写有效性**：比较误解、负面、对立回复、跑题和高频追问变化。
- **人设保持**：人工按原语气、观点保留、官方化程度和作者一致性打 1 至 5 分。

## 两人分工建议

- 成员 A：LangChain、Pydantic、提示词、六条 Agent 链、模型调试和 token 优化。
- 成员 B：Streamlit、SQLite、比例调整、潜水用户、热度、排序、历史和测试。
- 共同完成 Persona 模板、风险标准、案例、评测、报告与答辩。

## 局限

模拟不能精准预测真实舆论，风险等级也不是客观概率。LLM 可能产生同质化或偏负面的输出；改写后风险降低不表示真实发布一定不会引发争议。第一版不做事实核查，也不学习具体平台的评论语言。

## 参考

- [Social Simulacra](https://arxiv.org/abs/2208.04024)
- [S3](https://arxiv.org/abs/2307.14984)
- [OASIS](https://arxiv.org/abs/2411.11581)
- [LangChain Structured Output](https://docs.langchain.com/oss/python/langchain/structured-output)


# v1修改说明

## 在线服务器代码修改说明

修改目录：`/home/s2025013242/Big_task_v0`

### 1. `app.py`：前端布局与交互优化

- 将原文压力测试页面改成双栏布局：
  - 左侧展示内容分析、风险判断和修改方向。
  - 右侧展示模拟评论区。
- 将改写对比页改成左右对照布局，分别展示原文和改写后的分析结果。
- 按区块重新组织对比页，使以下内容左右水平对齐：
  - 原文与改写文本。
  - 综合风险判断。
  - 高风险原文与修改方向。
  - 可能的误解链。
  - 原文与改写后的模拟评论区。
- 将评论区改成多级树状结构，回复显示在父评论下方，并通过缩进和连接线体现层级关系。
- 评论卡片增加轮次、点赞数和回复数。
- 增加逐级返回按钮，支持返回输入页、受众确认页和原文结果页。
- 页面阶段切换后自动滚动到顶部。

### 2. `models/schemas.py`：扩展语义和评论数据结构

为 `ContentAnalysis` 增加：

- `content_type`：识别个人观点、日常分享、通知、道歉或推荐等内容类型。
- `tone`：记录帖子的平静、轻松或强烈等语气。
- `explicit_information`：保存帖子明确表达的信息。
- `reasonable_disagreements`：记录受众可能产生的合理分歧。
- `unsupported_inferences`：记录缺少原文依据的推断。

为 `CommentAction` 和 `Comment` 增加：

- `evidence_span`：记录评论实际回应的原文或父评论片段。
- `reaction_type`：记录支持、个人感受、第一印象、质疑、调侃、动机猜测等反应类型。

这些字段使评论不再只是孤立文本，而是能够追踪“针对什么内容、以什么方式作出反应”。新增字段均设置默认值，因此旧历史数据仍能正常加载。

### 3. `chains/content_analysis_chain.py`：增强帖子理解能力

- 内容分析不再只识别模糊词和风险句，还会判断帖子主题、内容类型和表达语气。
- 区分帖子明确表达的内容与模型自行推断的内容。
- 识别合理意见分歧，避免把普通个人偏好错误地当成沟通危机。
- 标记无文本依据的推断，为评论 Agent 限制不合理脑补。
- DEMO_MODE 也增加了对应的本地语义分析逻辑。

### 4. `chains/comment_chain.py`：重构评论生成逻辑

- 正式评论流程改为基于内容语义、Persona 属性和讨论上下文生成评论。
- 不再根据三个内置案例的关键词选择固定回答。
- 每个 Persona 会先确定关注内容和反应类型，再组织评论文本。
- 不同 Persona 可以产生支持、共鸣、第一印象、理性追问、谨慎修正、动机怀疑、观望、调侃和争议化概括等不同反应。
- 减少“信息没有说完整”“建议进一步说明”等高度重复的机械表达。
- 同一 Persona 在不同轮次再次参与时，不再重复相同评论。
- 后续轮次仍会产生新的顶级评论，同时把回复分散到不同讨论分支，避免所有回复集中在一条热评下。
- Live 模型提示词增加了语义依据、反应类型、多分支和防止无依据推断的约束。
- 精简请求字段，减少每轮评论生成的 Token 消耗。

### 5. `chains/audience_chain.py`：降低受众规划请求开销

- 不再向模型发送完整内容分析对象，只发送核心表达、内容类型、语气、涉及群体和少量潜在误解。
- Persona 模板只发送 ID、标签、所属群体和简短描述。
- Persona 的完整底层参数仍由本地模板保存，模型只负责规划权重。
- 这样既减少输入 Token，也降低结构化输出请求超时的概率。

### 6. `simulation/engine.py`：支持语义驱动的多轮模拟

- 原文和改写后的模拟都会接收对应版本的内容分析结果。
- 评论 Agent 能够在每轮生成时读取帖子语义信息。
- 评论创建后保存 `evidence_span` 和 `reaction_type`。
- 保留原有固定随机种子、Persona 激活、热度排序和潜水用户点赞机制。
- 支持多个顶级评论和多个回复分支。

### 7. `services/orchestrator.py`：调整完整业务流程

- 原文模拟时传入原文的内容分析结果。
- 改写后重新分析改写稿，并把新的分析结果传入二次模拟。
- 确保两次评论模拟分别依据各自文本，而不是共用原文语义。
- 保持相同 Persona、随机种子、轮数和热度规则，保证改写前后对比公平。

### 8. `services/database.py`：扩展评论持久化信息

- 在评论内部标签中保存 `evidence_span`。
- 保存评论的 `reaction_type`。
- 保留原有项目、Persona、评论、风险报告和模型缓存结构。
- 历史项目继续通过完整 JSON 快照恢复，不需要重新调用模型。

### 9. `tests/test_simulation.py`：增加真实性相关测试

新增测试用于检查：

- 自定义帖子可以形成多个顶级讨论。
- 回复会指向多个不同的父评论。
- 评论文本不会重复。
- “建议说明”“没有说完整”等机械句式不会集中出现。
- 每条评论都具有语义依据片段。
- 评论区包含多种不同反应类型。
- 内容分析能够识别个人观点类帖子。
- 固定随机种子下结果仍然可复现。

### 10. `.env`：模型运行配置调整

```env
DEMO_MODE=false
LLM_TIMEOUT=90
```

- 启用实时模型模式。
- 将请求超时从 30 秒提高到 90 秒。
- 配合请求体精简，解决受众规划 Agent 超时并使用本地降级结果的问题。

## 最终验证结果

- 远程环境全部自动测试通过：`27 passed`。
- 基础模型请求正常。
- 实时内容分析成功。
- 实时受众规划成功。
- `gateway_last_error` 为 `None`。
- Streamlit 健康检查返回 `ok`。
- 旧历史项目仍可打开；新逻辑需要通过“新建测试”生成新的结果。

# v2修改说明

# v3修改说明

## 1. 联网核对结果的失败隔离

- `services/web_research.py` 不再把无法解析为 JSON 的模型输出标记为成功。
- 非 JSON 或非对象结构统一返回 `failed`，并记录 `invalid_research_payload`。
- 结构化校验失败的文本不会作为事件结论展示，也不会注入任何后续 Agent。
- 联网失败仍不阻断原有分析和模拟流程，系统会在没有共享背景的情况下继续运行。

## 2. 所有相关 Agent 共享同一联网背景

- `services/orchestrator.py` 调整为先完成联网核对，再执行内容分析和受众规划。
- 通过同一个 `shared_background` 向以下 Agent 提供经过验证的精简背景：
  - 内容分析 Agent；
  - 受众规划 Agent；
  - 评论生成 Agent；
  - 风险诊断 Agent；
  - 人设保持改写 Agent；
  - 反事实对比 Agent。
- 改写前和改写后使用完全相同的联网背景，避免背景变化干扰反事实比较。
- 各 Agent 都必须区分已确认信息、当事方说法、争议说法和不确定信息，不得把不确定内容写成既定事实。
- 内容分析仍会区分帖子明确写出的信息和联网背景，避免把外部信息误记为原文内容。
- 改写 Agent 可以用背景避免事实冲突，但不得擅自把背景中的新断言添加到帖子。

## 3. 测试补充

- 增加非结构化联网输出被拒绝的测试。
- 增加非对象 JSON 被拒绝的测试。
- 增加完整 Agent 链共享同一背景的测试，覆盖内容分析、受众规划、原文与改写后的三轮评论、前后风险诊断、改写和对比阶段。
- 保留联网失败不注入背景以及结构化事实与来源绑定测试。

## 4. 前端兼容性

- 本次更新不修改 `app.py`。
- `PreparedProject`、`ProjectResult` 和联网核对结果字段保持兼容，现有页面无需同步调整。

## 5. 验证结果

- 服务器完整自动测试通过：`37 passed`。
- `git diff --check` 通过。
- 实际变更文件不包含 `app.py`。

## v3的其他修复

### 1. 改写正文泄漏防护

- `chains/rewrite_chain.py` 增加改写审计，拦截 `shared_background`、结构化字段名、编辑批注、修改理由和大量说明性括号进入 `rewritten_post`。
- 改写提示词明确要求 `rewritten_post` 只能是可直接发布的纯正文，说明性内容只能写入 `explanation`、`preserved_elements` 或 `repaired_risks`。
- 首次改写未通过审计时自动进入一次 `rewrite_repair`；仍失败时使用安全降级改写，避免把模型编辑过程展示给用户。

### 2. 改写质量门禁

- `services/orchestrator.py` 增加改写门禁：原文已经是低风险时不再强制改写，直接保留原文并说明原因。
- 对中高风险原文，改写后仍会做反事实模拟；如果改写后风险分没有下降，系统拒绝候选改写并保留原文，避免把更差版本当成最终建议。
- 该修复不修改 `app.py`，前端会继续使用原有四步流程展示后端返回结果。

### 3. 内容初步分析字段串位清洗

- `chains/content_analysis_chain.py` 增加内容分析清洗，确保 `ambiguous_phrases[].text` 只能是原帖中真实出现的短语。
- 清洗会丢弃 `核心表达`、`限定条件`、`内容类型`、`语气`、`shared_background` 等内部字段名或非原文片段，避免内部分类标签出现在“可能模糊的表达”列表中。
- 新增 `tests/test_output_guards.py` 覆盖改写泄漏拦截、干净正文放行、内容分析短语清洗、低风险跳过改写和改坏回退判定。

## v3界面优化

### 1. 侧边栏与基础视觉

- 左侧折叠栏改为深蓝色，并统一标题、说明、折叠图标、历史结果下拉框的对比度。
- “新建测试”和“打开历史结果”改为高对比度蓝色按钮，补充悬停、按下和禁用状态。
- 输入页拆分为“联网设置”“帖子内容”“发布者画像”三张卡片。
- 原四阶段进度条改为“输入内容 → 确认受众 → 原文结果 → 改写对比”步骤条，突出当前阶段和已完成阶段。
- 分析内容时保留原输入界面，并在原提交按钮位置显示工作提示。

### 2. 联网开关与参考来源

- 输入页增加“联网核对公开事件背景”开关，默认开启。
- 关闭联网后立即隐藏“事件线索”输入框，不执行搜索，也不向 Agent 注入外部背景。
- 取消参考来源最多展示 5 条的限制，完整展示本次搜索返回的全部来源。
- 对标题、域名、URL 和摘录做显示去重，避免同一网址重复出现。

### 3. 修改方向与正文展示

- 展示层会合并模型返回的跨行残句，过滤“改为”“建议”等孤立片段，保证“修改方向”语义完整。
- 原文和改写稿使用黑字浅底正文卡片，提升浅色页面中的可读性。
- 删除独立“高风险原文”板块；所有真实出现在帖子中的风险片段改为正文内高亮。
- 有误解链的风险片段悬停后展示风险原因和完整推导；没有误解链的风险片段仍展示风险原因，避免信息丢失。

### 4. 结构化误解链与悬停关联

- `models/schemas.py` 将误解链从字符串列表升级为 `source_span + steps` 的结构化数据，并兼容旧历史字符串。
- `chains/risk_chain.py` 要求 `source_span` 必须逐字复制对应帖子中的连续原文，并在代码层再次校验无效锚点。
- 新生成结果中的每条误解链都具有可定位的正文起点；旧历史中无法可靠恢复锚点的链不会被伪造关联。
- 悬停卡片放大字体，推导节点采用纵向排列，每个 `↓` 箭头单独占一行。
- 自动合并“读者将”“进而接受”等连接词残片，模型提示同时要求每个推导节点具有完整语义。
- 删除页面下方独立的“可能的误解链”板块，只保留正文高亮悬停交互。

### 5. 原文结果页与改写对比页统一

- 原文结果页顶部优先展示联网核对结论。
- 页面上方采用“左侧原文分析、右侧改写确认”双栏布局。
- 右栏允许用户直接编辑系统建议稿，并在文案下方通过主按钮进入改写对比。
- `services/orchestrator.py` 增加编辑稿重算流程：保留原文侧结果，只重新运行用户确认稿的内容分析、评论模拟、风险诊断和反事实比较。
- 改写对比页统一为原文与确认稿左右对照；对比结论、指标变化和“仍需注意”移动到两侧评论区之后。
- 原文结果页的评论区移到双栏下方并占满页面宽度；改写对比页继续保留左右评论对照。

### 6. 评论卡片

- 评论卡片保留 Persona、评论/回复类型和模拟轮次信息。
- 点赞数改为心形图标展示，回复数改为对话气泡图标展示。
- 小屏幕下元信息和互动数据自动换行，避免挤压评论正文。

### 7. 兼容与验证

- 旧历史项目可以继续加载；旧式误解链由 Schema 兼容迁移。
- Streamlit 页面测试覆盖输入开关、历史结果、原文结果、改写确认、对比页、悬停标记和评论区布局。
- 服务器完整自动测试通过：`42 passed`。
- Streamlit 8502 服务重启后健康检查返回 `ok`。
